[phases.setup]
nixpkgs = ["bun"]

[phases.install]
cmd = "bun install --frozen-lockfile"

[phases.build]
cmd = "bun run build"

[start]
cmd = "cd apps/server && bun run start"

[variables]
NODE_ENV = "production"
